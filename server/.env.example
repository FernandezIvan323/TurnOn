DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=tu_password
DB_NAME=appturnos
DB_TZ=America/Mexico_City
PORT=3001
HOST=0.0.0.0
# 1 = permite orígenes de red privada. 0 = solo CORS_ORIGINS
LAN_MODE=1
# 1 = confiar en X-Forwarded-* (detrás de reverse proxy)
TRUST_PROXY=0
JWT_SECRET=genera-un-secreto-aleatorio-con-node -e "crypto.randomBytes(64).toString('hex')"
CORS_ORIGINS=http://localhost:5180,http://127.0.0.1:5180
